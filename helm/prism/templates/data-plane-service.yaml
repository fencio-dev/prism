apiVersion: v1
kind: Service
metadata:
  name: {{ include "prism.fullname" . }}-data-plane
  labels:
    {{- include "prism.labels" . | nindent 4 }}
    app.kubernetes.io/component: data-plane
spec:
  type: ClusterIP
  selector:
    {{- include "prism.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: data-plane
  ports:
    - port: {{ .Values.dataPlane.port }}
      targetPort: {{ .Values.dataPlane.port }}
      protocol: TCP
